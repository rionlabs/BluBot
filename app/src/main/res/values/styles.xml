<resources>

    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryDark">@color/primaryDarkColor</item>
        <item name="colorAccent">@color/accentColor</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />

    <style name="AppTheme.Launch">
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@drawable/drawable_launch</item>
    </style>

    <style name="ControlBoard" />

    <style name="ControlBoard.Button">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_margin">@dimen/space_normal</item>
        <item name="fabCustomSize">@dimen/control_board_button_size</item>
    </style>

    <style name="ButtonStyle" parent="@style/Widget.MaterialComponents.Button" />

    <style name="ButtonStyle.OutlinedButton" parent="@style/Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:paddingStart">@dimen/space_large</item>
        <item name="android:paddingTop">@dimen/space_normal</item>
        <item name="android:paddingEnd">@dimen/space_large</item>
        <item name="android:paddingBottom">16dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@color/primaryDarkColor</item>
        <item name="android:textSize">@dimen/outlined_button_text_size</item>
        <item name="iconPadding">@dimen/space_normal</item>
        <item name="iconTint">@color/primaryDarkColor</item>
        <item name="rippleColor">@color/primaryLightColor</item>
        <item name="strokeWidth">1dp</item>
        <item name="strokeColor">@color/primaryDarkColor</item>
    </style>

</resources>